# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.pytest_cache/

# ── 虚拟环境 ────────────────────────────────────────────
.venv/
venv/
env/

# ── IDE / 编辑器 ────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── 项目数据 ────────────────────────────────────────────
# SQLite 数据库（数据文件，不提交）
data/*.db
data/*.db-wal
data/*.db-shm
data/query_history.json
# ESI 查询缓存（自动生成，不提交）
# 已迁移到 SQLite 数据库，旧 JSON 备份保留本地
data/id_name_cache.json
data/id_name_cache.json.bak
data/system_region_cache.json
data/system_region_cache.json.bak

# ── 临时文件 ────────────────────────────────────────────
*.log
.tmp/
temp/
